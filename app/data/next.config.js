/** @type {import('next').NextConfig} */
const nextConfig = {
  // This option enables static exports.
  output: 'export',
 
  // Optional: Change the default output directory `out` to `dist` or something else.
  // distDir: 'dist',
};
 
module.exports = nextConfig;